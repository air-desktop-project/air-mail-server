[package]
name = "ams-store"
description = "Stockage Maildir des messages"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
ams-config = { workspace = true }
ams-index = { workspace = true }
# `flock` : le verrou d'une boîte POP3. Un fichier témoin le ferait aussi, mais
# survivrait à un arrêt brutal — un verrou que le noyau relâche tout seul n'a
# pas ce défaut.
libc = { workspace = true }


[lints]
workspace = true
