[package]
name = "ams-sasl"
description = "SASL (RFC 4422) : le mécanisme PLAIN et son transport base64, sans entrée-sortie"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
# SHA-256, pour le HMAC de RFC 2104 et le PBKDF2 de RFC 5802. La crate reste
# sans entrée-sortie et sans allocation : `sha2` en `no_std` n'en fait aucune.
sha2 = { workspace = true }


[lints]
workspace = true
