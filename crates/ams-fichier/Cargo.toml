[package]
name = "ams-fichier"
description = "Poser un fichier : atomique pour qui le lit, durable pour la machine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
# `flock` : le verrou d'une lecture-modification-écriture. Un fichier témoin le
# ferait aussi, mais survivrait à un arrêt brutal — un verrou que le noyau
# relâche tout seul n'a pas ce défaut.
libc = { workspace = true }

[lints]
workspace = true
