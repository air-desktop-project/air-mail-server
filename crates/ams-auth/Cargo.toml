[package]
name = "ams-auth"
description = "Le magasin d'identifiants et la vérification Argon2id, sans entrée-sortie"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
ams-sasl = { workspace = true }
argon2 = { workspace = true }
# Le SCELLEMENT du vérificateur SCRAM (RFC 8439). DÉJÀ DANS L'ARBRE :
# `rustls-rustcrypto` la tire, et cette ligne ne fait que la déclarer là où on
# s'en sert. Aucune crate n'entre dans le graphe à cause d'elle.
chacha20poly1305 = { workspace = true }
p256 = { workspace = true }


[lints]
workspace = true
